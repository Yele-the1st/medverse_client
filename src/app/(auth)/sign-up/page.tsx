import { FC } from "react";
import SignUp from "@/components/SignUp";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <SignUp />
    </div>
  );
};

export default page;
